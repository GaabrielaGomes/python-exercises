# Python Exercises – AI & Analytics MBA

## 📌 About  
This repository contains NumPy exercises developed during my MBA in AI & Analytics for Python training purposes.  

Este repositório contém exercícios de NumPy desenvolvidos durante meu MBA em IA & Analytics para fins de treinamento em Python.

## 📚 Support Material  
The exercises were completed with support from the following video:  
https://youtu.be/LZDZ0AZmWHI?si=ZQZqAwOqhXr03Jyz  

Os exercícios foram realizados com apoio da seguinte videoaula:  
https://youtu.be/LZDZ0AZmWHI?si=ZQZqAwOqhXr03Jyz  

---

## 🧠 Exercises Overview  

1. Array creation with `arange`, `zeros`, `ones`, and `linspace`.  
   Criação de arrays com `arange`, `zeros`, `ones` e `linspace`.

2. 3D array creation and analysis of `shape`, `ndim`, and `size`.  
   Criação de array 3D e análise de `shape`, `ndim` e `size`.

3. Vector reshaping using `np.newaxis`.  
   Redimensionamento de vetor utilizando `np.newaxis`.

4. Boolean indexing and conditional filtering.  
   Indexação booleana e filtros condicionais.

5. Statistical operations (`max`, `min`, `mean`, `sum`).  
   Operações estatísticas (`max`, `min`, `mean`, `sum`).

6. Performance comparison: Python list vs NumPy array.  
   Comparação de performance: lista Python vs array NumPy.

7. Dice simulation using `default_rng()`.  
   Simulação de dados utilizando `default_rng()`.

8. Scatter plot generation with NumPy and Matplotlib.  
   Geração de gráfico de dispersão com NumPy e Matplotlib.

---

📌 *This repository is intended for educational and practice purposes only.*  
📌 *Este repositório é destinado apenas para fins educacionais e prática.*
